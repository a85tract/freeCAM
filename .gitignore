.venv/
__pycache__/
*.py[cod]
.pytest_cache/
build/
dist/
*.egg-info/
*.so
*.mod
*.o
native/kernels/
reference/
artifacts/
/pycam_*.o*
/logs/*
!/logs/.gitkeep

# Generated training datasets: runtime output, not source.
/examples/*.nc

# Trained models: a run's product, named by path when a stage loads one.
*.pt

# Site configuration: not committed.
site.env

# The Workflow Builder's front end: dependencies and build products.  The
# built page is shipped inside the wheel but not committed; `npm run build`
# under web/ produces it.
/web/node_modules/
/web/dist-pages/
/src/freecam/pi_cam/workflow_builder/static/
