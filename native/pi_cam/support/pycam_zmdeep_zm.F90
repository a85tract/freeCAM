! zm_conv_tend (zm_conv_intr.F90:514-906) hoisted for the
! zmdeep segment runner: its locals as module state, its body in pieces the
! runner calls in the source's order, its dummies bound by the caller.
!
! GENERATED by tools/generate_pi_cam_pausable_runners.py from
! native/pi_cam/pausable/deep_convection.yaml.  Do not edit by hand; edit the spec.
module pycam_zmdeep_zm
  use, intrinsic :: iso_c_binding, only: c_int, c_int32_t, c_int64_t, c_double, c_ptr, c_loc, c_null_ptr, c_f_pointer
  use shr_kind_mod, only: r8 => shr_kind_r8
  use physconst, only: cpair
  use ppgrid, only: pver, pcols, pverp, begchunk, endchunk
  use zm_conv, only: zm_conv_evap, zm_convr, convtran, momtran
  use cam_history, only: outfld, addfld, phys_decomp
  use perf_mod
  use cam_logfile, only: iulog
  use constituents, only: cnst_add
  use cam_history, only: outfld
  use physics_types, only: physics_state, physics_ptend
  use physics_types, only: physics_ptend_init, physics_update
  use physics_types, only: physics_state_copy, physics_state_dealloc
  use physics_types, only: physics_ptend_sum, physics_ptend_dealloc
  use phys_grid, only: get_lat_p, get_lon_p
  use time_manager, only: get_nstep, is_first_step
  use physics_buffer, only: pbuf_get_field, physics_buffer_desc, pbuf_old_tim_idx, pbuf_get_index
  use constituents, only: pcnst, cnst_get_ind, cnst_is_convtran1
  use check_energy, only: check_energy_chng
  use physconst, only: gravit
  use phys_control, only: cam_physpkg_is
  use water_types, only: iwtvap, iwtundef, iwtliq, iwtice, iwtcvrain, iwtcvsnow
  use water_tracer_vars, only: trace_water, wtrc_iatype, wtrc_nwset, wtrc_iawset, wtrc_srfpcp_indices, wtrc_bulk_indices
  use water_tracers, only: wtrc_check_h2o, wtrc_precip_evap, wtrc_q1q2_pjr
  use constituents, only: cnst_name
  use zm_conv_intr, only: mu, eu, du, md, ed, dp, dsubcld, jt, maxg, ideep, lengath, zmconv_org, ixorg, limcnv
  implicit none
  private
  public :: zm_piece_1, zm_piece_2, zm_piece_3, zm_piece_4, zm_piece_5, zm_piece_6, zm_convr_frame, zm_conv_evap_frame, momtran_frame, zm_convr_original, zm_conv_evap_original, momtran_original, zm_bind, zm_resolve_indices

  integer(c_int64_t), parameter :: zero_shape(1) = (/ 0_c_int64_t /)

  ! the routine's dummies, bound by the caller
  real(r8), pointer, save, public :: pblh(:) => null()
  real(r8), pointer, save, public :: mcon(:,:) => null()
  real(r8), pointer, save, public :: cme(:,:) => null()
  real(r8), pointer, save, public :: tpert(:) => null()
  real(r8), pointer, save, public :: dlf(:,:) => null()
  real(r8), pointer, save, public :: pflx(:,:) => null()
  real(r8), pointer, save, public :: zdu(:,:) => null()
  real(r8), pointer, save, public :: rliq(:) => null()
  real(r8), save, public :: ztodt
  real(r8), pointer, save, public :: jctop(:) => null()
  real(r8), pointer, save, public :: jcbot(:) => null()
  type(physics_state), pointer, save, public :: state => null()
  type(physics_ptend), pointer, save, public :: ptend_all => null()
  real(r8), pointer, save, public :: landfrac(:) => null()
  type(physics_buffer_desc), pointer, save, public :: pbuf(:) => null()
  real(r8), pointer, save, public :: wtdlf(:,:,:) => null()

  ! the routine's locals, held for the chunk in flight
  real(r8), save, target, public :: c0mask(pcols)
  real(r8), save, target, public :: cape(pcols)
  real(r8), pointer, save, public :: cld(:,:) => null()
  real(r8), save, target, public :: cu(pcols,pver)
  logical, save, target, public :: done(pcols,pver)
  real(r8), pointer, save, public :: dp_cldice(:,:) => null()
  real(r8), pointer, save, public :: dp_cldliq(:,:) => null()
  real(r8), save, target, public :: dz(pcols,pver)
  real(r8), save, target, public :: eps0(pcols)
  real(r8), pointer, save, public :: evapcdp(:,:) => null()
  real(r8), save, target, public :: evp(pcols,pver)
  real(r8), save, target, public :: evpstore(pcols,pver)
  real(r8), save, target, public :: fake_dpdry(pcols,pver)
  real(r8), pointer, save, public :: flxprec(:,:) => null()
  real(r8), pointer, save, public :: flxsnow(:,:) => null()
  real(r8), pointer, save, public :: fracis(:,:,:) => null()
  real(r8), save, target, public :: freqzm(pcols)
  real(r8), save, target, public :: ftem(pcols,pver)
  real(r8), save, target, public :: hmn(pcols,pver)
  real(r8), save, target, public :: hsat(pcols,pver)
  real(r8), save, target, public :: hsthat(pcols,pver)
  integer, save, target, public :: i
  real(r8), save, target, public :: icwd(pcols,pver,2)
  real(r8), save, target, public :: icwu(pcols,pver,2)
  integer, save, target, public :: ii
  logical, save, target, public :: isok
  integer, save, target, public :: itim_old
  integer, save, target, public :: ixcldice
  integer, save, target, public :: ixcldliq
  integer, save, target, public :: jd(pcols)
  integer, save, target, public :: k
  logical, save, target, public :: l_windt(2)
  integer, save, target, public :: lchnk
  integer, save, target, public :: lcl(pcols)
  integer, save, target, public :: lel(pcols)
  logical, save, target, public :: lq(pcnst)
  integer, save, target, public :: m
  real(r8), save, target, public :: md_out(pcols,pver)
  real(r8), save, target, public :: mu_out(pcols,pver)
  integer, save, target, public :: ncol
  integer, save, target, public :: nstep
  real(r8), save, target, public :: ntprprd(pcols,pver)
  real(r8), save, target, public :: ntsnprd(pcols,pver)
  real(r8), pointer, save, public :: org(:,:) => null()
  real(r8), pointer, save, public :: orgt(:,:) => null()
  real(r8), save, target, public :: pconb(pcols)
  real(r8), save, target, public :: pcont(pcols)
  real(r8), save, target, public :: pgdall(pcols,pver,2)
  real(r8), save, target, public :: pguall(pcols,pver,2)
  real(r8), pointer, save, public :: prec(:) => null()
  type(physics_ptend), target, save, public :: ptend_loc
  real(r8), save, target, public :: qd(pcols,pver)
  real(r8), save, target, public :: qds(pcols,pver)
  real(r8), pointer, save, public :: ql(:,:) => null()
  real(r8), save, target, public :: qst(pcols,pver)
  real(r8), save, target, public :: qsthat(pcols,pver)
  real(r8), save, target, public :: qu(pcols,pver)
  real(r8), save, target, public :: rpdpc(pcols,pver)
  real(r8), save, target, public :: rppe(pcols,pver)
  real(r8), pointer, save, public :: rprd(:,:) => null()
  real(r8), allocatable, save, target, public :: rwt(:,:,:,:)
  real(r8), save, target, public :: seten(pcols,pver)
  real(r8), pointer, save, public :: snow(:) => null()
  type(physics_state), save, target, public :: state1
  real(r8), save, target, public :: substore(pcols,pver)
  real(r8), save, target, public :: td(pcols,pver)
  real(r8), save, target, public :: tend_s_snwevmlt(pcols,pver)
  real(r8), save, target, public :: tend_s_snwprd(pcols,pver)
  real(r8), save, target, public :: tu(pcols,pver)
  real(r8), save, target, public :: wind_tends(pcols,pver,2)
  real(r8), save, target, public :: winds(pcols,pver,2)
  real(r8), save, target, public :: wtcu(pcols,pver)
  real(r8), save, target, public :: wtdu(pcols,pver)
  real(r8), save, target, public :: wted(pcols,pver)
  real(r8), save, target, public :: wteu(pcols,pver)
  real(r8), save, target, public :: wtevp(pcols,pver)
  real(r8), save, target, public :: wtmd(pcols,pver)
  real(r8), save, target, public :: wtmu(pcols,pver)
  real(r8), pointer, save, public :: wtprec(:) => null()
  real(r8), save, target, public :: wtprect(pcols,pcnst)
  real(r8), save, target, public :: wtrprd(pcols,pver,pcnst)
  real(r8), pointer, save, public :: wtsnow(:) => null()
  real(r8), save, target, public :: wtsnowt(pcols,pcnst)
  real(r8), pointer, save, public :: zm_org2d(:,:) => null()

  ! the module's private physics-buffer indices, resolved by name at configure
  integer, save, public :: cld_idx = -1
  integer, save, public :: dp_cldice_idx = -1
  integer, save, public :: dp_cldliq_idx = -1
  integer, save, public :: dp_flxprc_idx = -1
  integer, save, public :: dp_flxsnw_idx = -1
  integer, save, public :: fracis_idx = -1
  integer, save, public :: icwmrdp_idx = -1
  integer, save, public :: nevapr_dpcu_idx = -1
  integer, save, public :: prec_dp_idx = -1
  integer, save, public :: rprddp_idx = -1
  integer, save, public :: snow_dp_idx = -1

contains

  subroutine zm_bind(a_pblh, a_mcon, a_cme, a_tpert, a_dlf, a_pflx, a_zdu, a_rliq, a_ztodt, a_jctop, a_jcbot, a_state, a_ptend_all, a_landfrac, a_pbuf, a_wtdlf)
    ! the dummies of zm_conv_tend, as the caller's statement passes them
    real(r8), target, intent(inout) :: a_pblh(:)
    real(r8), target, intent(inout) :: a_mcon(:,:)
    real(r8), target, intent(inout) :: a_cme(:,:)
    real(r8), target, intent(inout) :: a_tpert(:)
    real(r8), target, intent(inout) :: a_dlf(:,:)
    real(r8), target, intent(inout) :: a_pflx(:,:)
    real(r8), target, intent(inout) :: a_zdu(:,:)
    real(r8), target, intent(inout) :: a_rliq(:)
    real(r8), intent(in) :: a_ztodt
    real(r8), target, intent(inout) :: a_jctop(:)
    real(r8), target, intent(inout) :: a_jcbot(:)
    type(physics_state), target, intent(inout) :: a_state
    type(physics_ptend), target, intent(inout) :: a_ptend_all
    real(r8), target, intent(inout) :: a_landfrac(:)
    type(physics_buffer_desc), target, intent(inout) :: a_pbuf(:)
    real(r8), target, intent(inout) :: a_wtdlf(:,:,:)
    pblh => a_pblh
    mcon => a_mcon
    cme => a_cme
    tpert => a_tpert
    dlf => a_dlf
    pflx => a_pflx
    zdu => a_zdu
    rliq => a_rliq
    ztodt = a_ztodt
    jctop => a_jctop
    jcbot => a_jcbot
    state => a_state
    ptend_all => a_ptend_all
    landfrac => a_landfrac
    pbuf => a_pbuf
    wtdlf => a_wtdlf
    if (.not. allocated(rwt)) allocate(rwt(pcols,pver,wtrc_nwset,2))
  end subroutine zm_bind

  subroutine zm_resolve_indices()
    ! the module's register/init indices, by the same field names (-1 where unregistered)
    use physics_buffer, only: pbuf_get_index
    integer :: ierr
    cld_idx = pbuf_get_index('CLD', ierr)
    dp_cldice_idx = pbuf_get_index('DP_CLDICE', ierr)
    dp_cldliq_idx = pbuf_get_index('DP_CLDLIQ', ierr)
    dp_flxprc_idx = pbuf_get_index('DP_FLXPRC', ierr)
    dp_flxsnw_idx = pbuf_get_index('DP_FLXSNW', ierr)
    fracis_idx = pbuf_get_index('FRACIS', ierr)
    icwmrdp_idx = pbuf_get_index('ICWMRDP', ierr)
    nevapr_dpcu_idx = pbuf_get_index('NEVAPR_DPCU', ierr)
    prec_dp_idx = pbuf_get_index('PREC_DP', ierr)
    rprddp_idx = pbuf_get_index('RPRDDP', ierr)
    snow_dp_idx = pbuf_get_index('SNOW_DP', ierr)
  end subroutine zm_resolve_indices

  subroutine zm_piece_1()
    ! zm_conv_intr.F90:514-565, verbatim

       lchnk = state%lchnk
       ncol  = state%ncol
       nstep = get_nstep()

       ftem = 0._r8
       mu_out(:,:) = 0._r8
       md_out(:,:) = 0._r8
       wind_tends(:ncol,:pver,:) = 0.0_r8

       call physics_state_copy(state,state1)             ! copy state to local state1.

       if(trace_water) then
         isOk = wtrc_check_h2o("before-deep", state1, state1%q, ztodt)
       end if

       lq(:) = .FALSE.
       lq(1) = .TRUE.
       if (zmconv_org) then
          lq(ixorg) = .TRUE.
       endif

       !water tracers:
       do ii=1,wtrc_nwset
         lq(wtrc_iatype(ii,iwtvap)) = .TRUE.
       end do

       call physics_ptend_init(ptend_loc, state%psetcols, 'zm_convr', ls=.true., lq=lq)! initialize local ptend type

    !
    ! Associate pointers with physics buffer fields
    !
       itim_old = pbuf_old_tim_idx()
       call pbuf_get_field(pbuf, cld_idx,         cld,    start=(/1,1,itim_old/), kount=(/pcols,pver,1/) )

       call pbuf_get_field(pbuf, icwmrdp_idx,     ql )
       call pbuf_get_field(pbuf, rprddp_idx,      rprd )
       call pbuf_get_field(pbuf, fracis_idx,      fracis, start=(/1,1,1/),    kount=(/pcols, pver, pcnst/) )
       call pbuf_get_field(pbuf, nevapr_dpcu_idx, evapcdp )
       call pbuf_get_field(pbuf, prec_dp_idx,     prec )
       call pbuf_get_field(pbuf, snow_dp_idx,     snow )

    !
    ! Begin with Zhang-McFarlane (1996) convection parameterization
    !
       call t_startf ('zm_convr')

       if (zmconv_org) then
          allocate(zm_org2d(pcols,pver))
          org => state%q(:,:,ixorg)
          orgt => ptend_loc%q(:,:,ixorg)
       endif


  end subroutine zm_piece_1

  subroutine zm_piece_2()
    ! zm_conv_intr.F90:580-722, verbatim



       call outfld('CAPE', cape, pcols, lchnk)        ! RBN - CAPE output
    !
    ! Output fractional occurance of ZM convection
    !
       freqzm(:) = 0._r8
       do i = 1,lengath(lchnk)
          freqzm(ideep(i,lchnk)) = 1.0_r8
       end do
       call outfld('FREQZM  ',freqzm          ,pcols   ,lchnk   )
    !
    ! Convert mass flux from reported mb/s to kg/m^2/s
    !
       mcon(:ncol,:pver) = mcon(:ncol,:pver) * 100._r8/gravit

       ! Store upward and downward mass fluxes in un-gathered arrays
       ! + convert from mb/s to kg/m^2/s
       do i=1,lengath(lchnk)
          do k=1,pver
             ii = ideep(i,lchnk)
             mu_out(ii,k) = mu(i,k,lchnk) * 100._r8/gravit
             md_out(ii,k) = md(i,k,lchnk) * 100._r8/gravit
          end do
       end do

       call outfld('ZMMU', mu_out(1,1), pcols, lchnk)
       call outfld('ZMMD', md_out(1,1), pcols, lchnk)

       ftem(:ncol,:pver) = ptend_loc%s(:ncol,:pver)/cpair
       call outfld('ZMDT    ',ftem           ,pcols   ,lchnk   )
       call outfld('ZMDQ    ',ptend_loc%q(1,1,1) ,pcols   ,lchnk   )
       call t_stopf ('zm_convr')

    !    do i = 1,pcols
    !    do i = 1,nco
       pcont(:ncol) = state%ps(:ncol)
       pconb(:ncol) = state%ps(:ncol)
       do i = 1,lengath(lchnk)
           if (maxg(i,lchnk).gt.jt(i,lchnk)) then
              pcont(ideep(i,lchnk)) = state%pmid(ideep(i,lchnk),jt(i,lchnk))  ! gathered array (or jctop ungathered)
              pconb(ideep(i,lchnk)) = state%pmid(ideep(i,lchnk),maxg(i,lchnk))! gathered array
           endif
           !     write(iulog,*) ' pcont, pconb ', pcont(i), pconb(i), cnt(i), cnb(i)
        end do
        call outfld('PCONVT  ',pcont          ,pcols   ,lchnk   )
        call outfld('PCONVB  ',pconb          ,pcols   ,lchnk   )

      call physics_ptend_init(ptend_all, state%psetcols, 'zm_conv_tend')
    !------------------------------------------
    !Water tracers
    !------------------------------------------

       if (trace_water) then
         call t_startf ('wtrc_zm_convr')

    !
    ! initialize tracer variables
    !       wtdlft(:ncol,:,:) = 0._r8
     !      wtcmet(:ncol,:,:) = 0._r8
     !      wtql(:ncol,:,:) = 0._r8
     !      wtqi(:ncol,:,:) = 0._r8
     !      wtqu(:ncol,:,:) = 0._r8
     !      wtqd(:ncol,:,:) = 0._r8
           wtdlf(:ncol,:,:) = 0._r8
           wtrprd(:ncol,:,:) = 0._r8
           wtprect(:ncol,:) = 0._r8
    !
    ! assign totals
    !       wtcmet(:ncol,:,1) = cme(:ncol,:)
    !       wtdlft(:ncol,:,1) = dlf(:ncol,:)
    !       wtql(:ncol,:,1) = ql(:ncol,:) ! partitioned to liquid on output
    !       wtqi(:ncol,:,1) = 0.0         ! partitioned to ice on output
    !       wtqu(:ncol,:,1) = qu(:ncol,:)
    !       wtqd(:ncol,:,1) = qd(:ncol,:)
           wtrprd(:ncol,:,1) = rprd(:ncol,:)
           wtprect(:ncol,1) = prec(:ncol)

    !       isOk = wtrc_check_h2o("before-deep", state, state%q, ztodt,ptend=ptend_loc) !check water tracers before

    !New (simplified) version three:

            call wtrc_q1q2_pjr( ptend_loc%q       ,ideep(:,lchnk), lengath(lchnk), &
                        state%q          ,qu      ,done  ,lel ,lcl , &
                        wtdu    ,du(:,:,lchnk)    ,dp(:,:,lchnk)   ,qst ,qsthat  , &
                        hmn     ,hsat  ,hsthat    ,wteu            , &
                        mu(:,:,lchnk)    ,md(:,:,lchnk)  ,qd ,tu  ,td , &
                        dsubcld(:,lchnk) ,maxg(:,lchnk)  ,jt(:,lchnk) ,ncol      , &
                        limcnv-1         ,wted    ,rppe  ,wtmu  ,wtmd , &
                        wtevp   ,evp     ,wtcu    ,cu    ,state%pmid  , &
                        dz      ,qds     ,rpdpc   ,c0mask,eps0        , &
                        jd      ,wtrprd  ,wtdlf   ,ztodt )

         call t_stopf ('wtrc_zm_convr')
       end if
    !--------------------------------------------------

      ! This name triggers a special case in physics_types.F90:physics_update()
      call physics_ptend_init(ptend_all, state%psetcols, 'convect_deep')

      ! add tendency from this process to tendencies from other processes
      call physics_ptend_sum(ptend_loc,ptend_all, ncol)

      ! update physics state type state1 with ptend_loc
      call physics_update(state1, ptend_loc, ztodt)

    !-----------------------
    !Check water tracer mass
    !-----------------------
    if (trace_water) then
      isOk = wtrc_check_h2o("after-deep zm", state1, state1%q, ztodt)
    end if
    !-----------------------

      ! initialize ptend for next process
      lq(:) = .FALSE.
      lq(1) = .TRUE.
      if (zmconv_org) then
         lq(ixorg) = .TRUE.
      endif

      !Water tracers:
      do ii=1,wtrc_nwset
        lq(wtrc_iatype(ii,iwtvap)) = .TRUE.
      end do

      call physics_ptend_init(ptend_loc, state1%psetcols, 'zm_conv_evap', ls=.true., lq=lq)

      call t_startf ('zm_conv_evap')
    !
    ! Determine the phase of the precipitation produced and add latent heat of fusion
    ! Evaporate some of the precip directly into the environment (Sundqvist)
    ! Allow this to use the updated state1 and the fresh ptend_loc type
    ! heating and specific humidity tendencies produced
    !

        call pbuf_get_field(pbuf, dp_flxprc_idx, flxprec    )
        call pbuf_get_field(pbuf, dp_flxsnw_idx, flxsnow    )
        call pbuf_get_field(pbuf, dp_cldliq_idx, dp_cldliq  )
        call pbuf_get_field(pbuf, dp_cldice_idx, dp_cldice  )
        dp_cldliq(:ncol,:) = 0._r8
        dp_cldice(:ncol,:) = 0._r8


  end subroutine zm_piece_2

  subroutine zm_piece_3()
    ! zm_conv_intr.F90:729-803, verbatim


        evapcdp(:ncol,:pver) = ptend_loc%q(:ncol,:pver,1)

         if (zmconv_org) then
             ptend_loc%q(:ncol,:pver,ixorg) = min(1._r8,max(0._r8,(50._r8*1000._r8*1000._r8*abs(evapcdp(:ncol,:pver))) &
                                              -(state%q(:ncol,:pver,ixorg)/10800._r8)))
             ptend_loc%q(:ncol,:pver,ixorg) = (ptend_loc%q(:ncol,:pver,ixorg) - state%q(:ncol,:pver,ixorg))/ztodt
         endif


    !----------------------------------------------------------
    ! Water tracers: evaporate falling precip, and assign state
    !----------------------------------------------------------
       if (trace_water) then
         !Check incoming water balance
    !     isOk = wtrc_check_h2o("before-evap", state, state%q, ztodt,ptend=ptend_loc)

         !Calculate water tracer rain evaporation (old version):
    !     call wtrc_zm_conv_evap(state1  , ptend_loc , wtrprd, cld , ztodt , &
    !                            flxprec, flxsnow, fevprec, fevsnow, ntprprd, ntsnprd, &
    !                            tend_s_snwprd, tend_s_snwevmlt, wtprect, wtsnowt, .false. )

          call wtrc_precip_evap(state1, wtrprd, ztodt, evpstore, substore, ptend_loc%q, wtprect, wtsnowt)

         !assign values to physics buffer variables
         do i=1,wtrc_nwset
           call pbuf_get_field(pbuf, wtrc_srfpcp_indices(iwtcvrain,i), wtprec)
           call pbuf_get_field(pbuf, wtrc_srfpcp_indices(iwtcvsnow,i), wtsnow)

           wtprec(:) = wtprect(:,wtrc_iatype(i,iwtvap)) - wtsnowt(:,wtrc_iatype(i,iwtvap)) !assign values (should be rain only)
           wtsnow(:) = wtsnowt(:,wtrc_iatype(i,iwtvap))                                    !(snow only)
         end do
       end if
    !----------------------------------------------------------

    !
    ! Write out variables from zm_conv_evap
    !
       ftem(:ncol,:pver) = ptend_loc%s(:ncol,:pver)/cpair
       call outfld('EVAPTZM ',ftem           ,pcols   ,lchnk   )
       ftem(:ncol,:pver) = tend_s_snwprd  (:ncol,:pver)/cpair
       call outfld('FZSNTZM ',ftem           ,pcols   ,lchnk   )
       ftem(:ncol,:pver) = tend_s_snwevmlt(:ncol,:pver)/cpair
       call outfld('EVSNTZM ',ftem           ,pcols   ,lchnk   )
       call outfld('EVAPQZM ',ptend_loc%q(1,1,1) ,pcols   ,lchnk   )
       call outfld('ZMFLXPRC', flxprec, pcols, lchnk)
       call outfld('ZMFLXSNW', flxsnow, pcols, lchnk)
       call outfld('ZMNTPRPD', ntprprd, pcols, lchnk)
       call outfld('ZMNTSNPD', ntsnprd, pcols, lchnk)
       call outfld('ZMEIHEAT', ptend_loc%s, pcols, lchnk)
       call outfld('CMFMCDZM   ',mcon ,  pcols   ,lchnk   )
       call outfld('PRECCDZM   ',prec,  pcols   ,lchnk   )


       call t_stopf ('zm_conv_evap')

       call outfld('PRECZ   ', prec   , pcols, lchnk)

      ! add tendency from this process to tend from other processes here
      call physics_ptend_sum(ptend_loc,ptend_all, ncol)

      ! update physics state type state1 with ptend_loc
      call physics_update(state1, ptend_loc, ztodt)

      !-----------------------
      !Check water tracer mass
      !-----------------------
      if (trace_water) then
        isOk = wtrc_check_h2o("after-evap", state1, state1%q, ztodt)
      end if
      !-----------------------


      ! Momentum Transport (non-cam3 physics)


  end subroutine zm_piece_3

  subroutine zm_piece_4()
    ! zm_conv_intr.F90:805-814, verbatim


         call physics_ptend_init(ptend_loc, state1%psetcols, 'momtran', ls=.true., lu=.true., lv=.true.)

         winds(:ncol,:pver,1) = state1%u(:ncol,:pver)
         winds(:ncol,:pver,2) = state1%v(:ncol,:pver)

         l_windt(1) = .true.
         l_windt(2) = .true.

         call t_startf ('momtran')

  end subroutine zm_piece_4

  subroutine zm_piece_5()
    ! zm_conv_intr.F90:820-851, verbatim

         call t_stopf ('momtran')

         ptend_loc%u(:ncol,:pver) = wind_tends(:ncol,:pver,1)
         ptend_loc%v(:ncol,:pver) = wind_tends(:ncol,:pver,2)
         ptend_loc%s(:ncol,:pver) = seten(:ncol,:pver)

         call physics_ptend_sum(ptend_loc,ptend_all, ncol)

         ! update physics state type state1 with ptend_loc
         call physics_update(state1, ptend_loc, ztodt)

         ftem(:ncol,:pver) = seten(:ncol,:pver)/cpair
         if (zmconv_org) then
            call outfld('ZM_ORG', state%q(:,:,ixorg), pcols, lchnk)
            call outfld('ZM_ORG2D', zm_org2d, pcols, lchnk)
         endif
         call outfld('ZMMTT', ftem             , pcols, lchnk)
         call outfld('ZMMTU', wind_tends(1,1,1), pcols, lchnk)
         call outfld('ZMMTV', wind_tends(1,1,2), pcols, lchnk)

         ! Output apparent force from  pressure gradient
         call outfld('ZMUPGU', pguall(1,1,1), pcols, lchnk)
         call outfld('ZMUPGD', pgdall(1,1,1), pcols, lchnk)
         call outfld('ZMVPGU', pguall(1,1,2), pcols, lchnk)
         call outfld('ZMVPGD', pgdall(1,1,2), pcols, lchnk)

         ! Output in-cloud winds
         call outfld('ZMICUU', icwu(1,1,1), pcols, lchnk)
         call outfld('ZMICUD', icwd(1,1,1), pcols, lchnk)
         call outfld('ZMICVU', icwu(1,1,2), pcols, lchnk)
         call outfld('ZMICVD', icwd(1,1,2), pcols, lchnk)


  end subroutine zm_piece_5

  subroutine zm_piece_6()
    ! zm_conv_intr.F90:853-906, verbatim


       ! Transport cloud water and ice only
       call cnst_get_ind('CLDLIQ', ixcldliq)
       call cnst_get_ind('CLDICE', ixcldice)

       lq(:)  = .FALSE.
       lq(2:) = cnst_is_convtran1(2:)

       !transport water tracer condensate (NOTE:  May be better to use is_convtran1 logical
       !instead -JN):
       if(trace_water) then
         do ii=1,wtrc_nwset
           lq(wtrc_iatype(ii,iwtliq)) = .TRUE.
           lq(wtrc_iatype(ii,iwtice)) = .TRUE.
         end do
       end if

       call physics_ptend_init(ptend_loc, state1%psetcols, 'convtran1', lq=lq)

       ! dpdry is not used in this call to convtran since the cloud liquid and ice mixing
       ! ratios are moist
       fake_dpdry(:,:) = 0._r8

       call t_startf ('convtran1')
       call convtran (lchnk,                                        &
                      ptend_loc%lq,state1%q, pcnst,  mu(:,:,lchnk), md(:,:,lchnk),   &
                      du(:,:,lchnk), eu(:,:,lchnk), ed(:,:,lchnk), dp(:,:,lchnk), dsubcld(:,lchnk),  &
                      jt(:,lchnk),maxg(:,lchnk), ideep(:,lchnk), 1, lengath(lchnk),  &
                      nstep,   fracis,  ptend_loc%q, fake_dpdry, Rwt)

       !Modify water tendencies via ratios:
       do m = 2,wtrc_nwset
         do k=1,pver
           do i=1,ncol
             ptend_loc%q(i,k,wtrc_iatype(m,iwtliq)) = Rwt(i,k,m,1)*ptend_loc%q(i,k,wtrc_iatype(1,iwtliq))
             ptend_loc%q(i,k,wtrc_iatype(m,iwtice)) = Rwt(i,k,m,2)*ptend_loc%q(i,k,wtrc_iatype(1,iwtice))
           end do
         end do
       end do

       call t_stopf ('convtran1')

       call outfld('ZMDICE ',ptend_loc%q(1,1,ixcldice) ,pcols   ,lchnk   )
       call outfld('ZMDLIQ ',ptend_loc%q(1,1,ixcldliq) ,pcols   ,lchnk   )

       ! add tendency from this process to tend from other processes here
       call physics_ptend_sum(ptend_loc,ptend_all, ncol)

       call physics_state_dealloc(state1)
       call physics_ptend_dealloc(ptend_loc)

       if (zmconv_org) then
          deallocate(zm_org2d)
       end if

  end subroutine zm_piece_6

  subroutine zm_convr_frame(ptrs, ndims, shapes, dtypes, intents, ncol_out)
    ! the paused `call zm_convr( lchnk ,ncol , state%t ,state%q(:,:,1), prec ,jctop ,jcb...` in the callee's argument order
    type(c_ptr), intent(inout) :: ptrs(:)
    integer(c_int), intent(inout) :: ndims(:), dtypes(:), intents(:)
    integer(c_int64_t), intent(inout) :: shapes(:,:)
    integer(c_int), intent(out) :: ncol_out
    integer(c_int32_t), save, target :: sc_1
    integer(c_int32_t), save, target :: sc_2
    real(c_double), save, target :: sc_3
    type(c_ptr) :: address
    ncol_out = int(ncol, c_int)
    sc_1 = int(lchnk, c_int32_t)
    call put_slot(1, c_loc(sc_1), 0, zero_shape, 2, 0, ptrs, ndims, shapes, dtypes, intents)
    sc_2 = int(ncol, c_int32_t)
    call put_slot(2, c_loc(sc_2), 0, zero_shape, 2, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(3, c_loc(state%t(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(4, c_loc(state%q(1,1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    if (associated(prec)) then
      call put_slot(5, c_loc(prec(1)), 1, (/ int(pcols, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    else
      call empty_slot(5, 1, 1, 1, ptrs, ndims, shapes, dtypes, intents)
    end if
    call put_slot(6, c_loc(jctop(1)), 1, (/ int(pcols, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(7, c_loc(jcbot(1)), 1, (/ int(pcols, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(8, c_loc(pblh(1)), 1, (/ int(pcols, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(9, c_loc(state%zm(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(10, c_loc(state%phis(1)), 1, (/ int(pcols, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(11, c_loc(state%zi(1,1)), 2, (/ int(pcols, c_int64_t), int(pver+1, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(12, c_loc(ptend_loc%q(1,1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(13, c_loc(ptend_loc%s(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(14, c_loc(state%pmid(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(15, c_loc(state%pint(1,1)), 2, (/ int(pcols, c_int64_t), int(pver+1, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(16, c_loc(state%pdel(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    sc_3 = real(.5_r8*ztodt, c_double)
    call put_slot(17, c_loc(sc_3), 0, zero_shape, 1, 2, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(18, c_loc(mcon(1,1)), 2, (/ int(pcols, c_int64_t), int(pverp, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(19, c_loc(cme(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(20, c_loc(cape(1)), 1, (/ int(pcols, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(21, c_loc(tpert(1)), 1, (/ int(pcols, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(22, c_loc(dlf(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(23, c_loc(pflx(1,1)), 2, (/ int(pcols, c_int64_t), int(pverp, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(24, c_loc(zdu(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    if (associated(rprd)) then
      call put_slot(25, c_loc(rprd(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    else
      call empty_slot(25, 2, 1, 1, ptrs, ndims, shapes, dtypes, intents)
    end if
    call slot_address_r8(mu(1,1,lchnk), address)
    call put_slot(26, address, 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(md(1,1,lchnk), address)
    call put_slot(27, address, 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(du(1,1,lchnk), address)
    call put_slot(28, address, 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(eu(1,1,lchnk), address)
    call put_slot(29, address, 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(ed(1,1,lchnk), address)
    call put_slot(30, address, 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(dp(1,1,lchnk), address)
    call put_slot(31, address, 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(dsubcld(1,lchnk), address)
    call put_slot(32, address, 1, (/ int(pcols, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_i4(jt(1,lchnk), address)
    call put_slot(33, address, 1, (/ int(pcols, c_int64_t) /), 2, 2, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_i4(maxg(1,lchnk), address)
    call put_slot(34, address, 1, (/ int(pcols, c_int64_t) /), 2, 2, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_i4(ideep(1,lchnk), address)
    call put_slot(35, address, 1, (/ int(pcols, c_int64_t) /), 2, 2, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_i4(lengath(lchnk), address)
    call put_slot(36, address, 0, zero_shape, 2, 2, ptrs, ndims, shapes, dtypes, intents)
    if (associated(ql)) then
      call put_slot(37, c_loc(ql(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 2, ptrs, ndims, shapes, dtypes, intents)
    else
      call empty_slot(37, 2, 1, 2, ptrs, ndims, shapes, dtypes, intents)
    end if
    call put_slot(38, c_loc(rliq(1)), 1, (/ int(pcols, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(39, c_loc(landfrac(1)), 1, (/ int(pcols, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    if (associated(org)) then
      call put_slot(40, c_loc(org(1,1)), 2, (/ int(size(org,1), c_int64_t), int(size(org,2), c_int64_t) /), 1, 2, ptrs, ndims, shapes, dtypes, intents)
    else
      call empty_slot(40, 2, 1, 2, ptrs, ndims, shapes, dtypes, intents)
    end if
    if (associated(orgt)) then
      call put_slot(41, c_loc(orgt(1,1)), 2, (/ int(size(orgt,1), c_int64_t), int(size(orgt,2), c_int64_t) /), 1, 2, ptrs, ndims, shapes, dtypes, intents)
    else
      call empty_slot(41, 2, 1, 2, ptrs, ndims, shapes, dtypes, intents)
    end if
    if (associated(zm_org2d)) then
      call put_slot(42, c_loc(zm_org2d(1,1)), 2, (/ int(size(zm_org2d,1), c_int64_t), int(size(zm_org2d,2), c_int64_t) /), 1, 2, ptrs, ndims, shapes, dtypes, intents)
    else
      call empty_slot(42, 2, 1, 2, ptrs, ndims, shapes, dtypes, intents)
    end if
    call put_slot(43, c_loc(qu(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(44, c_loc(qd(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(45, c_loc(dz(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(46, c_loc(rppe(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(47, c_loc(eps0(1)), 1, (/ int(pcols, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(48, c_loc(cu(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(49, c_loc(evp(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(50, c_loc(tu(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(51, c_loc(td(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(52, c_loc(jd(1)), 1, (/ int(pcols, c_int64_t) /), 2, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(53, c_loc(done(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 2, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(54, c_loc(lel(1)), 1, (/ int(pcols, c_int64_t) /), 2, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(55, c_loc(lcl(1)), 1, (/ int(pcols, c_int64_t) /), 2, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(56, c_loc(qst(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(57, c_loc(qsthat(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(58, c_loc(hmn(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(59, c_loc(hsat(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(60, c_loc(hsthat(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(61, c_loc(wteu(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(62, c_loc(wted(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(63, c_loc(wtdu(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(64, c_loc(wtmu(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(65, c_loc(wtmd(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(66, c_loc(wtcu(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(67, c_loc(c0mask(1)), 1, (/ int(pcols, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(68, c_loc(rpdpc(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(69, c_loc(qds(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(70, c_loc(wtevp(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
  end subroutine zm_convr_frame

  subroutine zm_convr_original()
    ! the original call, zm_conv_intr.F90:566-579, verbatim
       call zm_convr(   lchnk   ,ncol    , &
                        state%t       ,state%q(:,:,1),      prec    ,jctop   ,jcbot   , &
                        pblh    ,state%zm      ,state%phis    ,state%zi      ,ptend_loc%q(:,:,1)    , &
                        ptend_loc%s    , state%pmid     ,state%pint    ,state%pdel     , &
                        .5_r8*ztodt    ,mcon    ,cme     , cape,      &
                        tpert   ,dlf     ,pflx    ,zdu     ,rprd    , &
                        mu(:,:,lchnk),md(:,:,lchnk),du(:,:,lchnk),eu(:,:,lchnk),ed(:,:,lchnk)      , &
                        dp(:,:,lchnk) ,dsubcld(:,lchnk) ,jt(:,lchnk),maxg(:,lchnk),ideep(:,lchnk)   , &
                        lengath(lchnk) ,ql      ,rliq  ,landfrac,  &
                        org, orgt, zm_org2d,                       &
                        qu  ,qd  ,dz ,rppe   ,eps0 ,cu , &
                        evp ,tu  ,td  ,jd  ,done  ,lel      ,lcl ,qst ,qsthat     ,&
                        hmn ,hsat    ,hsthat      ,wteu     ,wted,wtdu,wtmu       ,wtmd ,wtcu , &
                        c0mask, rpdpc, qds ,wtevp )
  end subroutine zm_convr_original

  subroutine zm_conv_evap_frame(ptrs, ndims, shapes, dtypes, intents, ncol_out)
    ! the paused `call zm_conv_evap(state1%ncol,state1%lchnk, state1%t,state1%pmid,state...` in the callee's argument order
    type(c_ptr), intent(inout) :: ptrs(:)
    integer(c_int), intent(inout) :: ndims(:), dtypes(:), intents(:)
    integer(c_int64_t), intent(inout) :: shapes(:,:)
    integer(c_int), intent(out) :: ncol_out
    integer(c_int32_t), save, target :: sc_1
    integer(c_int32_t), save, target :: sc_2
    real(c_double), save, target :: sc_3
    ncol_out = int(ncol, c_int)
    sc_1 = int(state1%ncol, c_int32_t)
    call put_slot(1, c_loc(sc_1), 0, zero_shape, 2, 0, ptrs, ndims, shapes, dtypes, intents)
    sc_2 = int(state1%lchnk, c_int32_t)
    call put_slot(2, c_loc(sc_2), 0, zero_shape, 2, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(3, c_loc(state1%t(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(4, c_loc(state1%pmid(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(5, c_loc(state1%pdel(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(6, c_loc(state1%q(1,1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(7, c_loc(landfrac(1)), 1, (/ int(pcols, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(8, c_loc(ptend_loc%s(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 2, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(9, c_loc(tend_s_snwprd(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(10, c_loc(tend_s_snwevmlt(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(11, c_loc(ptend_loc%q(1,1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 2, ptrs, ndims, shapes, dtypes, intents)
    if (associated(rprd)) then
      call put_slot(12, c_loc(rprd(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    else
      call empty_slot(12, 2, 1, 0, ptrs, ndims, shapes, dtypes, intents)
    end if
    if (associated(cld)) then
      call put_slot(13, c_loc(cld(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    else
      call empty_slot(13, 2, 1, 0, ptrs, ndims, shapes, dtypes, intents)
    end if
    sc_3 = real(ztodt, c_double)
    call put_slot(14, c_loc(sc_3), 0, zero_shape, 1, 0, ptrs, ndims, shapes, dtypes, intents)
    if (associated(prec)) then
      call put_slot(15, c_loc(prec(1)), 1, (/ int(pcols, c_int64_t) /), 1, 2, ptrs, ndims, shapes, dtypes, intents)
    else
      call empty_slot(15, 1, 1, 2, ptrs, ndims, shapes, dtypes, intents)
    end if
    if (associated(snow)) then
      call put_slot(16, c_loc(snow(1)), 1, (/ int(pcols, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    else
      call empty_slot(16, 1, 1, 1, ptrs, ndims, shapes, dtypes, intents)
    end if
    call put_slot(17, c_loc(evpstore(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(18, c_loc(substore(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(19, c_loc(ntprprd(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(20, c_loc(ntsnprd(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    if (associated(flxprec)) then
      call put_slot(21, c_loc(flxprec(1,1)), 2, (/ int(pcols, c_int64_t), int(pverp, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    else
      call empty_slot(21, 2, 1, 1, ptrs, ndims, shapes, dtypes, intents)
    end if
    if (associated(flxsnow)) then
      call put_slot(22, c_loc(flxsnow(1,1)), 2, (/ int(pcols, c_int64_t), int(pverp, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    else
      call empty_slot(22, 2, 1, 1, ptrs, ndims, shapes, dtypes, intents)
    end if
  end subroutine zm_conv_evap_frame

  subroutine zm_conv_evap_original()
    ! the original call, zm_conv_intr.F90:723-728, verbatim
        call zm_conv_evap(state1%ncol,state1%lchnk, &
             state1%t,state1%pmid,state1%pdel,state1%q(:pcols,:pver,1), &
             landfrac, &
             ptend_loc%s, tend_s_snwprd, tend_s_snwevmlt, ptend_loc%q(:pcols,:pver,1), &
             rprd, cld, ztodt, prec, snow, &
             evpstore, substore, ntprprd, ntsnprd, flxprec, flxsnow)
  end subroutine zm_conv_evap_original

  subroutine momtran_frame(ptrs, ndims, shapes, dtypes, intents, ncol_out)
    ! the paused `call momtran (lchnk, ncol, l_windt,winds, 2, mu(1,1,lchnk), md(1,1,lch...` in the callee's argument order
    type(c_ptr), intent(inout) :: ptrs(:)
    integer(c_int), intent(inout) :: ndims(:), dtypes(:), intents(:)
    integer(c_int64_t), intent(inout) :: shapes(:,:)
    integer(c_int), intent(out) :: ncol_out
    integer(c_int32_t), save, target :: sc_1
    integer(c_int32_t), save, target :: sc_2
    integer(c_int32_t), save, target :: sc_3
    integer(c_int32_t), save, target :: sc_4
    integer(c_int32_t), save, target :: sc_5
    integer(c_int32_t), save, target :: sc_6
    real(c_double), save, target :: sc_7
    type(c_ptr) :: address
    ncol_out = int(ncol, c_int)
    sc_1 = int(lchnk, c_int32_t)
    call put_slot(1, c_loc(sc_1), 0, zero_shape, 2, 0, ptrs, ndims, shapes, dtypes, intents)
    sc_2 = int(ncol, c_int32_t)
    call put_slot(2, c_loc(sc_2), 0, zero_shape, 2, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(3, c_loc(l_windt(1)), 1, (/ int((2), c_int64_t) /), 2, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(4, c_loc(winds(1,1,1)), 3, (/ int(pcols, c_int64_t), int(pver, c_int64_t), int((2), c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    sc_3 = int(2, c_int32_t)
    call put_slot(5, c_loc(sc_3), 0, zero_shape, 2, 0, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(mu(1,1,lchnk), address)
    call put_slot(6, address, 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(md(1,1,lchnk), address)
    call put_slot(7, address, 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(du(1,1,lchnk), address)
    call put_slot(8, address, 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(eu(1,1,lchnk), address)
    call put_slot(9, address, 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(ed(1,1,lchnk), address)
    call put_slot(10, address, 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(dp(1,1,lchnk), address)
    call put_slot(11, address, 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_r8(dsubcld(1,lchnk), address)
    call put_slot(12, address, 1, (/ int(pcols, c_int64_t) /), 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_i4(jt(1,lchnk), address)
    call put_slot(13, address, 1, (/ int(pcols, c_int64_t) /), 2, 0, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_i4(maxg(1,lchnk), address)
    call put_slot(14, address, 1, (/ int(pcols, c_int64_t) /), 2, 0, ptrs, ndims, shapes, dtypes, intents)
    call slot_address_i4(ideep(1,lchnk), address)
    call put_slot(15, address, 1, (/ int(pcols, c_int64_t) /), 2, 0, ptrs, ndims, shapes, dtypes, intents)
    sc_4 = int(1, c_int32_t)
    call put_slot(16, c_loc(sc_4), 0, zero_shape, 2, 0, ptrs, ndims, shapes, dtypes, intents)
    sc_5 = int(lengath(lchnk), c_int32_t)
    call put_slot(17, c_loc(sc_5), 0, zero_shape, 2, 0, ptrs, ndims, shapes, dtypes, intents)
    sc_6 = int(nstep, c_int32_t)
    call put_slot(18, c_loc(sc_6), 0, zero_shape, 2, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(19, c_loc(wind_tends(1,1,1)), 3, (/ int(pcols, c_int64_t), int(pver, c_int64_t), int((2), c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(20, c_loc(pguall(1,1,1)), 3, (/ int(pcols, c_int64_t), int(pver, c_int64_t), int((2), c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(21, c_loc(pgdall(1,1,1)), 3, (/ int(pcols, c_int64_t), int(pver, c_int64_t), int((2), c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(22, c_loc(icwu(1,1,1)), 3, (/ int(pcols, c_int64_t), int(pver, c_int64_t), int((2), c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(23, c_loc(icwd(1,1,1)), 3, (/ int(pcols, c_int64_t), int(pver, c_int64_t), int((2), c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
    sc_7 = real(ztodt, c_double)
    call put_slot(24, c_loc(sc_7), 0, zero_shape, 1, 0, ptrs, ndims, shapes, dtypes, intents)
    call put_slot(25, c_loc(seten(1,1)), 2, (/ int(pcols, c_int64_t), int(pver, c_int64_t) /), 1, 1, ptrs, ndims, shapes, dtypes, intents)
  end subroutine momtran_frame

  subroutine momtran_original()
    ! the original call, zm_conv_intr.F90:815-819, verbatim
         call momtran (lchnk, ncol,                                        &
                       l_windt,winds, 2,  mu(1,1,lchnk), md(1,1,lchnk),   &
                       du(1,1,lchnk), eu(1,1,lchnk), ed(1,1,lchnk), dp(1,1,lchnk), dsubcld(1,lchnk),  &
                       jt(1,lchnk),maxg(1,lchnk), ideep(1,lchnk), 1, lengath(lchnk),  &
                       nstep,  wind_tends, pguall, pgdall, icwu, icwd, ztodt, seten )
  end subroutine momtran_original

  subroutine slot_address_r8(field, address)
    ! a TARGET dummy so c_loc is legal whatever the actual argument's attributes
    ! (a module array of the driver's own module, say); a contiguous actual is
    ! passed without a copy, and an element stands for the rest of its array
    real(r8), intent(inout), target :: field(*)
    type(c_ptr), intent(out) :: address
    address = c_loc(field(1))
  end subroutine slot_address_r8

  subroutine slot_address_i4(field, address)
    integer(c_int32_t), intent(inout), target :: field(*)
    type(c_ptr), intent(out) :: address
    address = c_loc(field(1))
  end subroutine slot_address_i4

  subroutine put_slot(index, address, rank, shape, dtype, intent, ptrs, ndims, shapes, dtypes, intents)
    integer, intent(in) :: index, rank, dtype, intent
    type(c_ptr), intent(in) :: address
    integer(c_int64_t), intent(in) :: shape(:)
    type(c_ptr), intent(inout) :: ptrs(:)
    integer(c_int), intent(inout) :: ndims(:), dtypes(:), intents(:)
    integer(c_int64_t), intent(inout) :: shapes(:,:)
    integer :: axis
    ptrs(index) = address
    ndims(index) = int(rank, c_int)
    shapes(:, index) = 0_c_int64_t
    do axis = 1, rank
      shapes(axis, index) = shape(axis)
    end do
    dtypes(index) = int(dtype, c_int)
    intents(index) = int(intent, c_int)
  end subroutine put_slot

  subroutine empty_slot(index, rank, dtype, intent, ptrs, ndims, shapes, dtypes, intents)
    ! an argument with no storage in this call: a null address and zero extents
    integer, intent(in) :: index, rank, dtype, intent
    type(c_ptr), intent(inout) :: ptrs(:)
    integer(c_int), intent(inout) :: ndims(:), dtypes(:), intents(:)
    integer(c_int64_t), intent(inout) :: shapes(:,:)
    ptrs(index) = c_null_ptr
    ndims(index) = int(rank, c_int)
    shapes(:, index) = 0_c_int64_t
    dtypes(index) = int(dtype, c_int)
    intents(index) = int(intent, c_int)
  end subroutine empty_slot

end module pycam_zmdeep_zm
