{
 "schema_version": 1,
 "what": "A zero-writing kernel with instratus_condensate's arguments in shadow (second attempt).",
 "pbs_job_id": "7402202.desched1",
 "run_tag": "instratus-zero-numba-shadow",
 "kernel_models": {
  "instratus_condensate": {
   "file": "instratus_zero.py",
   "function": "instratus_zero",
   "binding": "numba",
   "shadow": true,
   "path": "examples/plugins/numba_kernels/instratus_zero.py"
  }
 },
 "job": "validation/jobs/pi_cam_pausable_50step.pbs",
 "python_stages": [
  "cloud_macro_microphysics"
 ],
 "native_image": "build/pi_cam_promoted_p20",
 "ranks": 512,
 "steps_completed": 0,
 "resources": {
  "queue": "main",
  "placement": "4 exclusive nodes, 128 ranks each"
 },
 "outcome": "every rank raised at the stage's installation, before the first step: the installed process's payload hashed differently across the ranks (PythonProcessContractError); no summary",
 "diagnosis": "The pickled NativePlugin carried the compiled code's address and the process id, which differ on every rank, and the install compares the payload hash across ranks.  The pickle now carries only a rank-invariant identity (hook, source file, function, source hash, mode); the address is found again in the process's own registry when the stage is unpickled.",
 "disposition": "Resubmitted under new tags on the same p20 image (fice-plugin2-live, fice-plugin2-shadow, instratus-zero-plugin2-shadow); the job's comparison and health files of the aborted runs are kept with this record and the tags are not reused.",
 "bfb_record_note": "pi_cam_pausable_instratus-zero-numba-shadow_vs_oracle_50step_bfb.json and the .health.json were written by the job's comparison of a run that never started stepping; kept with this record."
}