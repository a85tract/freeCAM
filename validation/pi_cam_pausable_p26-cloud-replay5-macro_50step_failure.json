{
 "schema_version": 1,
 "job": "validation/jobs/pi_cam_pausable_50step.pbs",
 "python_stages": [
  "cloud_macro_microphysics"
 ],
 "native_image": "build/pi_cam_promoted_p26",
 "ranks": 512,
 "resources": {
  "queue": "develop",
  "placement": "4 whole nodes, 128 ranks each",
  "memory_requested": "4 x 235 GB"
 },
 "bfb_record_note": "the job's comparison (.bfb.json) and health (.health.json) files of the failed run are kept with this record",
 "outcome": "step 1 ran; on step 2 the replay found an input whose live lanes did not hash as the capture's and fell over reporting it: AttributeError, 'BlockReplay' object has no attribute 'mismatches'",
 "diagnosis": "the replay object restored on each rank by pickle (the stage is cloudpickled into the process registry) carried its directory and count but not the mismatch list a fresh one has; __setstate__ now gives it one.  The finding itself -- an input differing at step 2 -- is what the rerun is to name.",
 "steps_completed": 1,
 "run_tag": "p26-cloud-replay5-macro",
 "pbs_job_id": "7452423.desched1",
 "what": "The same capture replayed through the macrophysics slot, the microphysics original verifying its outputs against the record.",
 "disposition": "rerun as p26-cloud-replay6-macro on the same capture"
}